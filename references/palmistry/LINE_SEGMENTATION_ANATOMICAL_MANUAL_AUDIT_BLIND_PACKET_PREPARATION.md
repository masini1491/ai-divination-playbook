# Palm Line Segmentation — Blind Anatomical Manual Audit Packet Preparation

Status: **REFERENCE-ONLY / BLIND PACKET PREPARED / ANNOTATION NOT YET FROZEN**

This record captures preparation of the blind manual-audit packet after the anatomical manual-audit dry-run passed.

## Repository state

The local repository was updated to:

```text
afbf4e7ed0310d1f26007c2dce25f22e8b3b0e2b
```

before packet generation.

## Preparation command

```text
python references/palmistry/line_segmentation_anatomical_manual_audit_runner.py \
  --model ~/palm-detector-study/palm-line-reader/models/student_fp32.onnx \
  --meta ~/palm-detector-study/palm-line-reader/models/model_meta.json \
  --zip /mnt/c/Users/user/Documents/X/MOHI_sample_10p_3s_5i.zip \
  --mediapipe-results /mnt/c/Users/user/Documents/X/MOHI_repeatability_results.json \
  --spatial-results ~/palm-detector-study/results/MOHI_line_segmentation_spatial_structure_control.json \
  --packet-dir ~/palm-detector-study/results/MOHI_line_segmentation_anatomical_manual_audit_blind_packet \
  --prepare-blind
```

## Observed preparation accounting

All ten frozen sources were prepared successfully:

```text
P001/S1/01.jpg
P002/S1/01.jpg
P003/S1/01.jpg
P004/S1/01.jpg
P005/S1/01.jpg
P006/S1/01.jpg
P007/S1/01.jpg
P008/S1/01.jpg
P009/S1/01.jpg
P010/S1/01.jpg
```

The runner reported:

```text
BLIND PACKET READY
/home/user/palm-detector-study/results/MOHI_line_segmentation_anatomical_manual_audit_blind_packet
/home/user/palm-detector-study/results/MOHI_line_segmentation_anatomical_manual_audit_blind_packet/annotate.html
```

The packet preparation therefore passed its source/input reproduction gates sufficiently to emit all ten blind images and the annotation UI.

## Blinding boundary

The packet was generated by `--prepare-blind`, which intentionally emits blind corrected baseline images and annotation tooling without model overlays. Annotation has not yet been frozen or compared against model output.

A subsequent attempt to launch the local HTML through Windows PowerShell from the current WSL shell failed with:

```text
cannot execute binary file: Exec format error
```

This occurred after `BLIND PACKET READY` and is an environment/WSL interop launch failure, not a packet-generation failure. It does not alter the blind packet, its images, annotation template, or the model-sealed procedural boundary.

## Next evidence gate

The next step is manual blind annotation only. After all ten images and three classes per image are assigned `observable`, `uncertain`, or `not_observable`, the exported `annotations.json` must be SHA256-frozen before any `--compare` execution or model-overlay review.

Palmistry remains **REFERENCE-ONLY / DRAFT / NOT PRODUCTION-ROUTABLE**.
